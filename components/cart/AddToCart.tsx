'use client'; import {ShoppingCart} from 'lucide-react'; import {Product} from '@/lib/types'; import {useCart} from './CartContext'; import {useState} from 'react'; export function AddToCart({product}:{product:Product}){ const {add}=useCart(); const [added,setAdded]=useState(false); return (<button className={`btn ${added?'bg-green-500 text-white':'bg-brand-600 text-white hover:bg-brand-700'} shadow-soft`} onClick={()=>{add(product,1); setAdded(true); setTimeout(()=>setAdded(false),1500);}}><ShoppingCart className='w-4 h-4 mr-2'/> {added?'Ajouté !':'Ajouter au panier'}</button>); }