import Link from 'next/link'; import { Product } from '@/lib/types'; export function ProductCard({product}:{product:Product}){return (<Link href={`/product/${product.slug}`} className='card overflow-hidden block group border-brand-100 hover:shadow-lg'><div className='aspect-[4/5] overflow-hidden bg-brand-50'><img src={product.image} alt={product.name} className='w-full h-full object-cover group-hover:scale-[1.02] transition' /></div><div className='p-4'><div className='flex items-center justify-between gap-3'><h3 className='font-medium'>{product.name}</h3><span className='text-sm text-gray-700'>{product.price.toFixed(2)} €</span></div><div className='mt-2 text-xs text-gray-600'>{product.technique} · {product.format} · {product.orientation}</div></div></Link>);}